version: '3.7'

services:
  beagledocs:
    image: 10.8.0.102:443/beaglesystems/docs:develop
    container_name: beagledocs
    restart: always
    ports:
      - 5000:5000
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
